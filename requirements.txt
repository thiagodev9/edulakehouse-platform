loguru==0.7.3
PyYAML==6.0
requests==2.32.3
python-dotenv==1.0.1
pandas==2.1.3
pyarrow==21.0.0
pyspark==4.0.3
delta-spark==4.0.1
pytest==8.3.0
pytest-cov==5.0.0

# Apache Airflow runs via Docker — no official Windows support.
# See docker-compose.yml to start Airflow + MinIO + Postgres locally.
